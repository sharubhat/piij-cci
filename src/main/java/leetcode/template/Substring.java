package leetcode.template;

public class Substring {
  /*
  public int findSubstring(String s) {
    int[] count = new int[128];
    int counter; // to check whether the substring is valid
    int begin, end; // pointers, one to point to head and other to point to tail
    int d; // the length of substring

    for (char c : s.toCharArray()) {
      // initialize the map here
      count[c]++;
    }

    while (end < s.length()) {
      if (count[s.charAt(end++)]-- ?) {
        // modify the counter here
      }
      while (// counter condition) {
        // update d here if finding minimum
        // increase begin to make it invalid / valid again
        if (count[s.charAt(begin++)]++ ?) {
          // modify the counter here
        }
        // update d here if finding maximum
    }
  }
  */
}
